import SwiftUI

/// Haliac — the easiest possible drawing experience.
///
/// "We don't make mistakes, just happy little accidents." — Bob Ross
@main
struct HaliacApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
